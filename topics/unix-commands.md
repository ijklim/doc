# Common Unix Commands

```sh
# Check OS
cat /etc/issue

# Find files
find / -name *.so

# List files with permission info
ls -la /var/www

# =========================
# php
# =========================

# Find extension folder
php -i | grep extension_dir


```